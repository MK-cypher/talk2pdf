import {PDFDocumentType} from "@/lib/type";
import {DialogContent} from "./ui/dialog";

export default function SelectDocs({allDocs}: {allDocs: PDFDocumentType[]}) {
  return <DialogContent></DialogContent>;
}
